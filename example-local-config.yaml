server:
  port: 3000
  base-url: /gitvillance
db:
  description: base de prueba
  host: localhost
  port: 5432
  database: gitvillance_db
  schema: gitvillance
  user: gitvillance_admin
  password: cambiar_esta_clave
install:
  dump:
    drop-his: false
    db:
      owner: gitvillance_owner
client-setup:
  lang: es
devel:
  delay: 0
log:
  db:
    on-demand: true
gitvillance:
  local-repo: ../local-repo-gitvillance
  github-token: github_pat_readonly-token_from https://github.com/settings/tokens?type=beta
  # usuario de git por defecto por host (clave <hostname>-username), para que el credential manager no pregunte cual cuenta usar
  # github.com-username: mi-usuario-github
  # gitlab.com-username: mi-usuario-gitlab
